import type { PersistentEmployeeDaemon } from "../daemon/persistent-employee-daemon.js";

export function registerDefaultCollaborationActions(
  _daemon: PersistentEmployeeDaemon,
): void {
  // State-changing collaboration exits are handled by scene final-result tools.
}
